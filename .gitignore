node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*

# Raw fetched data: large and reproducible via `npm run fetch:all`.
# The register snapshot IS checked in (see data/raw/.gitignore) so builds
# are reproducible without network access.
data/raw/bin-raw/
